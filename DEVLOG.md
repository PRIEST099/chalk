# DEVLOG

## 2026-07-15 Phase 0 — Scaffold & hygiene
Built: Next.js App Router scaffold with strict TypeScript and Tailwind; Zustand dependency; modern chalkboard shell layout; MIT license; environment template; README compliance skeleton; typecheck script.
[HUMAN DECISION] Start the repository history with the specification files before scaffolding, preserving the product brief as the first commit.
[CODEX DECISION] Used the App Router scaffold with a responsive static shell so Phase 1 can add the core pipeline without revisiting app structure.
Codex speedups worth mentioning: Initialized the repository, scaffolded the app, established the project hygiene files, and verified the first runnable shell in one phase pass.
Problems hit and how resolved: The workspace name had uppercase letters, which npm rejects as a package name; scaffolded in a temporary lowercase folder then moved the generated project into the prescribed root.

[HUMAN DECISION] Use the Diagram Director prompt from SPEC.md verbatim; any change requires product-owner sign-off.
[CODEX DECISION] Use the Responses API with `responses.parse` and Zod Structured Outputs because the model is returning application data rather than invoking application tools; the documented `gpt-5.6` alias is retained as the environment default.
[CODEX DECISION] Structured Outputs requires every response-schema property to be required; represented DiagramOp's optional properties as nullable in the model-only Zod schema, then normalized null to undefined at the API route boundary so the public operation contract remains unchanged.

## 2026-07-17 Phase 1 — Core loop via typed input
Built: Server-only Diagram Director route with strict structured-output and reference validation; Zustand diagram and undo state; React Flow canvas; first-class typed input; water-cycle sample path and malformed-operation resilience coverage.
[CODEX DECISION] The API schema fix above keeps OpenAI's required-field constraint isolated at the server boundary while preserving the public DiagramOp contract.
[HUMAN DECISION] Phase 1 acceptance was manually verified with the water cycle, correction, no-op chit-chat, re-explanation highlight, and undo behaviors.
Codex speedups worth mentioning: Built the validated model-to-canvas loop, resilient operation handling, and focused automated schema tests alongside the interactive path.
Problems hit and how resolved: Structured Outputs rejected optional Zod fields; used required nullable model fields and immediate normalization, documented above.

## 2026-07-17 Phase 2 — Live speech
Built: Web Speech API implementation behind TranscriptSource; final/interim transcript display; request coalescing and cancellation; automatic recognition restart; typed-input fallback; live status states; server-side token-usage/session-total console logs.
[CODEX DECISION] Keep Web Speech isolated behind TranscriptSource so a browser-native live source and TypedInput share one batching and interpretation pipeline.
[HUMAN DECISION] Voice behavior is approved; typed testing isolates the observed voice misfires to speech-recognition noise, so the Diagram Director prompt remains unchanged.
Codex speedups worth mentioning: Added the continuous-speech lifecycle, browser-permission recovery paths, and rate-conscious batching without altering the typed core loop.
Problems hit and how resolved: Chrome may end continuous recognition unexpectedly; restart only while listening remains explicitly enabled, while Stop aborts both recognition and pending interpretation work.

## 2026-07-17 Phase 3 — Layout & visual polish
Built: ELK-driven diagram positioning for flow and timeline layouts; deterministic bounded cycle placement based on ELK ordering; 500ms position and viewport transitions; node-kind accents/icons; stronger highlight glow; high-contrast edge labels; WWI sample alongside the water-cycle sample.
[CODEX DECISION] Use ELK layered layout for reliable ordering, then place cycle nodes in a bounded circular arrangement because ELK's radial algorithm overflowed on cyclic input in the browser build.
[HUMAN DECISION] Prioritize readable edge labels and clearly differentiated node types in the dark-board visual language.
Codex speedups worth mentioning: Added the layout engine, visual vocabulary, sample-driven verification, and browser screenshots in one focused polish phase.
Problems hit and how resolved: React Flow initially fit the temporary node positions before asynchronous layout resolved; added a post-layout viewport fit and bounded cycle coordinates.

[CODEX DECISION] Phase 3 punch-list verification: live typed probes passed—correction removed `evaporation`; Sun/Ocean produced exactly one `sun-heats-ocean` edge and no node; re-mentioning Evaporation returned a highlight without a duplicate. No Diagram Director prompt change was needed.

## 2026-07-17 Phase 4 — Gestures
Built: In-browser MediaPipe HandLandmarker PiP with landmark overlay; throttled and smoothed point cursor; debounced pinch selection; open-palm swipe undo; pointerContext submission; complete mouse and keyboard parity; camera-denied typed/mouse fallback.
[CODEX DECISION] Keep landmark processing in requestAnimationFrame and mutate the cursor DOM ref directly, avoiding React render storms while constraining inference to 30fps.
[HUMAN DECISION] Add self-edge rejection plus solid smoothstep edge labels and post-layout fit-to-view as the Phase 4 opening punch list.
Codex speedups worth mentioning: Connected gesture recognition, pointer-aware model context, and the no-camera control path without compromising the existing typed/speech pipeline.
Problems hit and how resolved: Browser hand tracking cannot be verified without an active physical camera; the app exposes a friendly explicit opt-in and remains completely usable through mouse/keyboard controls.

[CODEX DECISION] Map smoothed hand coordinates into the canvas element's bounding rectangle—not the full browser viewport—and align the axis with the unmirrored PiP, preventing the gesture cursor from appearing only when the finger leaves the expected board area.

[CODEX DECISION] Gesture defect root causes: viewport-pixel hit testing ignored React Flow transforms, the PiP/cursor mirror directions differed, repeated pinches had no exit hysteresis, and radial auto-fit could run after manual navigation. Fixed with Flow-space hit testing, matched mirroring, hand-size-normalized pinch enter/exit thresholds plus cooldowns, open-palm velocity-gated swipe, and a manual-viewport grace period.
[CODEX DECISION] Swipe velocity must use the mirrored hand axis, matching the PiP and cursor coordinate system; using the raw camera axis inverted the left-swipe detector.

## 2026-07-17 Phase 5 — Replay, export, handout, persistence
Built: Server-only `/api/summarize` handout generation with per-call and running token logging; Markdown handout download; dark PNG and portable JSON export/import; debounced local session restore; clear confirmation; and an isolated replay scrubber with play, pause, step, and exit controls.
[CODEX DECISION] Rebuild replay from the immutable ops log into a separate display diagram, so replay never mutates or risks losing the live board.
[HUMAN DECISION] Require the handout to stay grounded exclusively in the final diagram and transcript, including a diagram-specific recap, glossary, and answered comprehension questions.
Codex speedups worth mentioning: Added the second GPT-5.6 route, durable session artifacts, and a presentation-friendly replay path while preserving the existing typed, voice, and gesture loop.
Problems hit and how resolved: The existing board component had accumulated tightly coupled render state; separated the replay display state from live state and deferred restore/playback state changes to avoid React effect cascades.

[CODEX DECISION] PNG export converts `html-to-image`'s generated data URL back into an image Blob before download; wrapping the URL string itself produced a text payload mislabeled as a PNG.

## 2026-07-17 Phase 6 — Deploy & judge kit
Built: Public-release README with the required product, GPT-5.6, architecture, collaboration, setup, and judge-test sections; public-demo rate limits; production-build preparation; and GitHub/Vercel handoff materials.
[CODEX DECISION] Use lightweight in-memory per-IP route limits (20 interpret and 5 handout calls per minute) to protect the public demo balance while remaining generous for normal judging; production verification remains pending the hosted URL.
[HUMAN DECISION] Keep GitHub creation and Vercel environment-variable entry under the product owner’s control, with Codex providing precise release instructions and later production QA.
Codex speedups worth mentioning: Audited every reachable commit and every local Git blob object for common API-key patterns, completed the release documentation, and removed the build-time external font dependency for offline-reliable builds.
Problems hit and how resolved: The full object-store audit found one unreachable `.env.example` blob containing a key pattern; it was pruned before release. The initial production build could not fetch Google Fonts in this environment; replaced the remote build dependency with local system font fallbacks.

[CODEX DECISION] PNG export captures React Flow's renderer rather than its outer container, excluding the webcam video and landmark canvas that `html-to-image` cannot safely serialize.

[HUMAN DECISION] Direct production retest confirmed the typed correction behavior works; the earlier automated browser observation was a timing false negative, so no Diagram Director prompt change is authorized or needed.

## 2026-07-17 Side task — Demo video kit and pre-recording fixes
Built: Remotion demo-video hardening — real repo URL in the end card; per-segment narration files with recording ducking replacing the all-or-nothing voiceover switch; render-time validation (duration cap, trim/playbackRate coverage, missing media, silent segments, placeholder URLs) with a REMOTION_ALLOW_PLACEHOLDERS draft override; font loading through @remotion/fonts so renders block until the local woff2 is ready; media-duration probing in the manifest script; per-segment caption positions so captions stop covering the app's bottom UI; letterboxing instead of cropping for non-16:9 captures; dev script now regenerates the manifest; stale placeholder render deleted; footage/audio gitignored. App fixes — replay now lays out each step with the same ELK pipeline as the live board (the ops log carries no positions, so replay previously showed fallback stair-step positions), and the camera-denied message from the hand tracker is now surfaced in the control-bar alert instead of being discarded.
[HUMAN DECISION] Review the project and demo-video setup against the recording script, apply the resulting fixes, and commit nothing in this session.
Verified: typecheck and lint pass in both projects; a draft still renders with the placeholder override and fails loudly without it; replay was exercised in the running app — intermediate steps use real layout and the final step matches the live board exactly, with the live board intact after exit.
Problems hit and how resolved: the root ESLint began scanning the untracked remotion/ tree and flagged its patterns; remotion/ is now ignored at the root (it has its own toolchain) and the flagged frame-accumulation pattern was replaced with precomputed segment start frames.

[HUMAN DECISION] Recordings must appear embedded as components inside the branded video frame — never full-bleed — and the narration script must exist as timed on-screen subtitles that double as the read-along text when recording voiceover against Remotion Studio playback.
Follow-up built: browser-style frame (chrome bar showing the live URL) hosting every recording, with a hero split layout for the hook; per-segment timed subtitle cues replacing the static captions, validated against overlap/overrun; media-duration probing verified end-to-end with a generated test clip (embedded rendering confirmed, too-short-source warning fired, test clip removed). Review findings fixed: entering replay now stops listening and aborts any in-flight interpretation, and undo via Ctrl/Cmd+Z or palm swipe is gated off during replay so the hidden live board can no longer mutate mid-replay.

[HUMAN DECISION] The water-cycle board must read as a true circle; refine the drawing logic.
Layout refinement built: cycle layouts now place node centers on an ellipse whose radius is computed from real node widths (no more fixed 115x75 ring that piled 130-180px nodes on top of each other), ring order follows the directed edges from their source (Sun to Ocean to Evaporation to Condensation to Precipitation) instead of sorting ELK's vertical output by x, the radial hint gained a genuine hub-and-spoke placement (highest-degree node centered), ring layouts draw curved bezier edges instead of stepped ones, and the node-width heuristic is now shared between layout and gesture hit-testing. Verified live: the water-cycle sample lays out as a clean pentagon ring with edges flowing around it, and the WWI sample still reads left-to-right. Demo-video review fixes also landed: playbackRate removed from the timeline model (its trim math encoded Remotion semantics wrongly and every segment runs 1x), subtitle-cue problems downgraded to draft warnings so Studio stays usable while retiming, crossfade instead of blink at abutting cue boundaries, the wide frame resized to clear two-line subtitles, and unprobeable media durations now flagged instead of silently skipping checks.

[HUMAN DECISION] Hand gestures must work flawlessly when tracking is enabled; harden the gesture path.
Gesture hardening built: eliminated a 30fps React re-render storm (debug samples reached setState even with the HUD off, and pointer updates re-dispatched identical store values every camera frame — both now guarded, keeping landmark processing off the render path as SPEC requires); gesture hit-testing now uses React Flow's measured node dimensions with the shared heuristic as fallback, so annotated (taller) nodes point correctly; the gesture cursor is hidden until tracking is active, centers exactly on the fingertip, and disappears with cleared pointer state when tracking stops; MediaPipe WASM and model URLs pinned to the installed 0.10.35 version instead of @latest (both pinned URLs verified reachable). Verified against live store state in the running app: hover-to-point, click-to-select with FIFO max-2 and toggle-deselect, pointer clearing on mouse-out, live Ctrl/Cmd+Z undo, undo correctly inert during replay, and the camera-denied path now shows its friendly fallback message end-to-end. A dev-only window handle exposing the board store was added for this kind of state-level testing; it is compiled out of production.

Gesture follow-up from live camera testing and adversarial review: MediaPipe's benign "INFO: Created TensorFlow Lite XNNPACK delegate" line is filtered from console.error so the Next.js dev overlay stops presenting successful tracker startup as an error; hit-testing now reads React Flow's internal measured node sizes (getInternalNode — the getNodes() path never carries measurements in controlled mode, so tall annotated nodes were still mis-targeted); losing the hand for ~half a second now hides the cursor and releases pointer state so a stale node is never sent to the model after the teacher lowers their hand; the gesture path only clears pointer state it set itself, so an idle pointing hand or a denied camera enable can no longer wipe mouse hover; interrupted camera setups (unmount, disable, or re-click mid-initialization) release the media stream instead of leaving the webcam captured; and per-frame debug work is skipped entirely unless the dev HUD is on.

## 2026-07-17 Post-Phase 6 - Interaction and accessibility polish
Built: visibly distinct pointer and selection states (warm outer ring for pointed, accent fill plus check badge for selected); reversible batch history with a Redo control, Ctrl/Cmd+Shift+Z, and Ctrl/Cmd+Y; and a first-class final-transcript plain-text download carrying the lesson title and subject.
[HUMAN DECISION] Strengthen the visual clarity of the gesture demo, let a judge step forward after undoing, and make the accessibility promise durable with an exported transcript.
[CODEX DECISION] Keep redo at the same atomic DiagramOp-batch granularity as undo and retain the fully laid-out diagram snapshot, so redo restores the exact visual board rather than re-running model operations or layout.
Problems hit and how resolved: the active localhost port was occupied by the independent Remotion Studio, so main-app UI verification was not run against that port; static verification completed cleanly instead.
Verified: `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` pass.

[HUMAN DECISION] The demo video needs premium animation and transitions.
Motion pass built: adopted @remotion/transitions (pinned 4.0.490) so every segment boundary is a 0.5s eased spring crossfade instead of a hard cut — the composition now resolves to 4980 frames (166s, still under the 175s cap) since each of the 8 transitions overlaps its neighbors. Elevated per-element motion throughout: a living background with a slow amber light sweep and 20 drifting chalk-dust motes, browser frames that lift in with a clearing blur and float gently on a sine, staggered blur-in text reveals for cards and the hero (with an animating accent underline), subtitles that rise into place, and faintly colored traffic-light dots on the frame chrome. All motion is deterministic (Math.sin-seeded, no Math.random/Date), so renders are reproducible. Verified by rendering stills: hero layout, a mid-crossfade frame showing both segments compositing with the incoming card mid blur-in, and the resolved composition duration (166.00s).

## 2026-07-17 Post-Phase 6 - Lesson Room presenter view
Built: Restyled the default workspace as a live Lesson Room: a minimal lesson header and timer, polished real-presenter camera/avatar tile with editable teacher name and live mic states, Captions sidebar, inviting empty-board hero, collapsible Type instead drawer, and a centered circular control bar that groups export, replay, sample, debug, import, and board actions without fake participants.
[HUMAN DECISION] Presentation-layer redesign before final deploy: Chalk must read as a live lesson room rather than a testing tool, while the interpret pipeline, Diagram Director prompt, gestures, replay, and exports remain unchanged.
[CODEX DECISION] Make the presenter tile a controlled view of the existing HandTracker stream rather than opening a second camera source; this preserves landmark rendering, camera permission recovery, and the one-stream rule while allowing the toolbar to own its on/off state.
Verified: current water-cycle sample produced the five-node cycle in the Lesson Room; the Type instead drawer, export menu, and overflow controls were exercised in-browser. `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` pass. Before/after browser screenshots were captured.
Problems hit and how resolved: the old dense button row obscured the teaching surface; actions now live in labelled circular controls with text tooltips and grouped menus, while typed input remains first-class but stays out of the presenter’s way when listening.

[HUMAN DECISION] The Lesson Room must fit the viewport without scrolling on a laptop, and be responsive down to mobile — overriding SPEC's "no mobile" non-goal for the presentation layer only (pipeline, prompt, gestures, replay, exports unchanged).
Responsive layout pass: root shell switched from min-h-screen to a fixed h-[100dvh] flex column so the board/captions region fills remaining space via flex-1/min-h-0 instead of a min-h-[calc(100vh-10rem)] that (with the padded container and header) overflowed the viewport and pushed the floating control bar below the fold. Two-column breakpoint lowered from xl(1280) to lg(1024) so typical laptops get side-by-side rather than stacking tall; below lg the board flexes and the captions panel becomes a fixed 160px strip. Header is now single-row (nowrap) with the subject field hidden under sm and title/timer/status held on one line. Control bar and round buttons scale down on mobile (size-10, tighter gaps, max-width guard) so they never overflow narrow screens; replay bar spans the board width on mobile; the presenter/camera tile is compact on small screens; the empty-state hero biases upward on mobile to clear the corner tile. Verified in-browser at 1366x768, 768x1024, and 375x667: zero document scroll on both axes and the control bar within the fold at every size; typecheck, lint, and production build pass.

[HUMAN DECISION] The bottom control bar must sit beside the camera/presenter tile (to its left) instead of overlapping it.
Control-bar placement fix: on large screens (lg+) the earlier lg:p-5 override had removed the bottom space reservation, so the centered control bar landed on top of the camera tile (verified overlap at 1440x900). The control bar is now right-anchored on lg+ to sit just left of the presenter tile (lg:right-[38.5rem], lg:bottom-10, left-auto, translate-none) — both are right-anchored so the ~11-15px gap holds across widths — while below lg it stays centered at the bottom where it already clears the camera. Verified in-browser: no overlap and no document scroll at 1024x700, 1440x900, and 390x844; typecheck and lint pass.

[HUMAN DECISION] Move the "Type instead" input into the Captions panel (at its bottom) so typing and the caption text live in one place, and drop the separate floating drawer.
Typed-input relocation: the floating typed drawer and its toolbar "Type" toggle were removed; the typed input (label + textarea + Draw button) now sits as a persistent row at the bottom of the Captions aside, below the scrollable transcript. The transcript area is min-h-0 flex-1 so it absorbs the remaining height; on mobile the panel grew to h-60 with overflow-hidden and the textarea caps at max-h-20 (sm:max-h-28) so the input stays inside the card. The typedDrawerOpen state and its start/stop-listening toggles were removed, and the empty-state hint copy updated to point at the Captions panel. Verified in-browser: input renders inside the aside on desktop and mobile, Draw enables only with text, floating drawer gone, Type toggle gone, no document scroll at 1440x900 or 390x844; typecheck and lint pass.

## 2026-07-17 Post-Phase 6 - Public API cost hardening
Built: Added an origin/referer allowlist for the production Chalk URL plus localhost; bounded transcript, compact-diagram, pointer, and handout payload shapes; explicit Responses API output-token ceilings; and a best-effort daily per-IP cap alongside the existing minute caps. Added automated coverage for each guard.
[HUMAN DECISION] Final hardening pass before submission: cost-abuse protection only, with zero-friction judge access and no product feature changes.
[CODEX DECISION] Reject headerless API POSTs as well as cross-origin ones because the same-origin browser fetch used by Chalk supplies Origin, while allowing missing headers would leave the public routes trivially callable from scripts.
[CODEX DECISION] Keep the daily cap in the existing in-memory limiter and document it as best-effort because serverless instances do not share state; it is a low-cost abuse brake, not a distributed quota system.
Verified: production's current 20/min interpret and 5/min handout limits returned 429 on requests 21 and 6 respectively using invalid bodies only (no OpenAI calls). Local hardening tests cover trusted and rejected origins, payload limits, output ceilings, minute/daily limits, and friendly errors. Typecheck, lint, tests, and production build pass; the production client bundle contains no Debug HUD hooks or configured OpenAI key.
Problems hit and how resolved: the docs MCP is unavailable in this environment, so the current official OpenAI Responses reference was checked through the official developer site; it confirms `max_output_tokens` bounds visible and reasoning output.

[CODEX DECISION] Exclude the standalone `remotion/` project from the root Next.js TypeScript program: Vercel installs only root dependencies, while the video project owns its separate dependencies and `tsconfig`, so root deployment type-checking must not resolve Remotion imports.

[HUMAN DECISION] Make the public README setup instructions cross-platform rather than assuming a Windows shell.

[HUMAN DECISION] The video opening must explain the problem to a general audience, not only judges: keep the cold-open hook, but expand the problem card from 6s to 11s with universal phrasing ("a live diagram any student can follow") and drop the Build Week name-drop from the spoken track (it remains on screen in the hook title block and end card).
Demo-video problem statement expanded: problem card now carries three lines and three narration cues; vo-problem.mp3 script updated (max 11s); card text muting now applies only to the end card's URL lines so the problem card's punchline renders in full contrast. Content duration 175s (at the internal cap), rendered 171s (2:51) — verified via remotion compositions (5130 frames) and a rendered still of the new card; typecheck and lint pass.

[HUMAN DECISION] Hand tracking regressions block recording: the pointer visibly enters a node without it highlighting, and pinching drags the pointer downward off the target. Diagnose carefully and fix both.
Gesture pointing root cause found empirically: the gesture cursor's position:fixed context was being re-rooted to the canvas section, so the drawn cursor sat exactly (section.left, section.top) ≈ (+13px, +77px) away from the client point used for hit-testing — the user aimed the visible dot at a node while the internal point checked empty space 77px below (React Flow's screenToFlowPosition contract itself was verified correct against the installed source, and the flow-space hit test passed at the true node center). Fix: the cursor is now absolutely positioned and anchored against its measured offsetParent rect every frame, immune to whichever ancestor creates the positioning context; verified in-browser at 0.0px placement error (previously +13/+77). Pinch reliability: closing the fingers physically drags the index fingertip down, so the tracker now freezes cursor emission while a pinch is forming (normalized pinch distance < 0.5, releasing at > 0.55), and selection gained a 600ms grace window — a pinch with nothing currently pointed selects the node the cursor was over moments ago, which also makes quick point-then-pinch work without waiting out the full dwell. Pointing itself is more forgiving on camera: dwell reduced 400ms to 300ms, hit-boxes padded by 12 flow px, and brief hand-jitter exits (<180ms) no longer reset the dwell or drop the pointed state. Typecheck and lint pass; mouse paths unchanged.

[HUMAN DECISION] Recording is blocked by two issues: Remotion misbehaves with the dropped-in screen recordings, and Download handout returns 400. Diagnose both.
Diagnosed and fixed: (1) The Remotion issue was a stale media manifest — hook.mp4 and segment-b-gestures.mp4 were added after the last prepare-media run, so generated-media.ts did not list them and both segments rendered placeholder slates despite the files being present; regenerating the manifest fixed playback (probes confirmed all three files are H.264/yuv420p/30fps/AAC; segment-a 57.6s and hook 8.01s fit their trim windows, while segment-b is 22.42s against a 30s window and was captured at 1360x764, so it needs a re-take at 1920x1080 of at least 30s — noting it also predates the gesture-pointing fix). (2) The handout 400 was reproduced against the hardened /api/summarize schema: an emptied lesson title (min 1), a title over 160 chars, or a subject hint over 120 chars each 400 — the exact standard payload returns 200 with a real handout — and the client swallowed the server's message behind a generic retry prompt. Fix: the client now clamps title (fallback "Untitled lesson", max 160), subject hint (max 120), and transcript (most-recent 16k) to the server contract before sending, surfaces the server's actual error text in the toast (rate-limit and daily-cap messages now reach the user), and the title/subject inputs gained matching maxLength attributes. Verified end-to-end in the running app: blank title + real Download handout button now succeeds; typecheck, lint, and the full test suite pass.

[HUMAN DECISION] Keep the 22.4s gestures take rather than re-recording, and add a new ~7s segment showing the downloaded handout in an editor to compensate; source the README hero from an image-generation prompt.
Timeline restructure for the delivered footage: the two newest recordings had crossed filenames (frame extraction showed "handout.mp4" contained the gestures take and "segment-b-gestures-2.mp4" contained the VS Code handout display) — files renamed to match their contents and the manifest regenerated. Gestures segment trimmed 30s to 22s with retimed cues; artifacts trimmed 25s to 22s (its 22.89s source) and its closing line moved to a new 7s "handout" recording segment (file handout.mp4, narration vo-handout.mp3, cue "Recap, glossary, three questions — nothing invented, all from what was actually taught."). Content now 171s across 10 segments; rendered 166.5s (4995 frames, verified via remotion compositions); stills confirm the real gestures and VS Code footage render embedded in the browser frame. vo-artifacts.mp3 max is now 22s (three sentences); typecheck and lint pass.

[HUMAN DECISION] The README hero should be the same footage as the video's hook, not a generated image.
Hero GIF produced with Remotion's own GIF codec by rendering the composition's hook segment (frames 0-239 at half scale, 10fps): docs/hero.gif (6MB, 960x540, 8s) — identical framing to the video's cold open (title block, browser frame, subtitle). README placeholder replaced with the image.

[HUMAN DECISION] The README hero should be a purpose-built Remotion GIF that explains the product in a few seconds — without touching the ChalkDemo composition.
Built src/HeroGif.tsx as a fully self-contained second composition (registered additively in Root.tsx; imports nothing from timeline/validate/ChalkDemo, whose 4995-frame render is verified unchanged): an 8-second loop where live captions "speak" the water-cycle lesson word by word beside a pulsing Listening chip while the five-node ring draws itself — spring node entrances, curved edges tracing in with labels, a staggered finale glow, and the closing line "You talk. The board draws." before the board wipes clean for a seamless loop. All motion deterministic; brand colors, dust field, and Space Grotesk match the demo video. Rendered natively with Remotion's GIF codec to docs/hero.gif (960x540, 10fps, 2.6MB — down from 6MB of screen footage); README alt text updated. Typecheck and lint pass; verified via rendered stills at build, mid, and finale moments.

[HUMAN DECISION] The hero GIF must present the overall product, not repeat the demo video's water-cycle explanation.
HeroGif rebuilt as a 10.4s three-beat montage (still fully self-contained; ChalkDemo verified unchanged at 4995 frames): beat one draws a spoken HISTORY lesson as a timeline layout (1914 - War begins, Alliance system, 1918 - Armistice, Treaty of Versailles) to show Chalk maps any subject; beat two replays the multimodal signature — the amber gesture cursor glides in, dwell-rings and pinch-selects two nodes (checkmark badges) and the spoken "Connect these two." draws the edge; beat three slides in a paper Class-handout card (recap/glossary/3-questions skeleton with Replay/PNG/Handout chips) before the amber closing line "You talk. The board draws." and the loop wipe. Edge-label geometry fix: horizontal timeline edges arc higher (bow -120) and labels sit at 0.9x the bow displacement past the curve apex so wide node cards can never clip them (verified in stills). Rendered to docs/hero.gif (960x540, 10fps, 3.4MB); README alt text updated; typecheck and lint pass.

[HUMAN DECISION] The pushed README renders with errors on GitHub; correct them.
README rendering fixes: the Architecture mermaid diagram failed GitHub's parser because labels beginning with "/" open mermaid's parallelogram shape token — "/api/interpret" and "/api/summarize" (plus the other slash-bearing labels) are now quoted strings, which is the canonical mermaid escape. The Judge Testing Guide still described the pre-restyle UI; every step now names the real Lesson Room controls — sample loading, Undo/Redo, Clear board, and Load JSON via the "More lesson controls" (three-dots) menu, typed corrections via the "Type instead" box and its "Draw" button in the Captions panel, "Replay lesson"/"Exit replay" in the dock, "Download handout"/"Export PNG"/"Save JSON"/"Download transcript" in the Export menu, the mic button labeled "Start listening", and the camera button "Enable hand tracking camera" with the presenter tile described in place of the old PiP wording.

[HUMAN DECISION] Mute recordings whose audio adds nothing, design the video's audio around generated narration (keeping only live moments like "connect these two"), fix the blank ending, and spend remaining budget on a clearer opening problem statement.
Audio redesign and timing pass: the blank tail was a stray `from={-337}` on the TransitionSeries (committed in the finalize commit) shifting all content 11.2s early — removed, and the end card verified on screen at the final frames. Ducking is now narration-window based: a recording ducks to 0.15 only while its narration file is actually playing (probed duration + 0.2s), then returns to full volume — so vo-gestures.mp3 narrates the gesture setup while the live "connect these two" stays fully audible; unknown durations fall back to whole-segment ducking. The artifacts, handout, and build-story recordings are muted outright (silent screen captures; narration carries them). The problem card grew 11s to 14s with four cues adding the visual-learners/notes-vanish stakes. Content now 174s (5085 frames rendered = 169.5s = 2:49.5, inside the 2:55 target); typecheck and lint pass; README narration tables updated.

[HUMAN DECISION] Review the delivered ElevenLabs audio, fit it into the timeline, and eliminate the Studio MediaPlaybackError.
Audio-fit pass: the Studio playback crash was the build-story segment trimming 0-30s against a 29.08s source — native playback crossing 29.08s threw the browser MediaPlaybackError (the validator had been warning about this exact file; Studio only console-warns). Segment trimmed to 29s and a partial render across the formerly crashing frames completes cleanly. All eight delivered MP3s probe as valid 44.1kHz mp3 (no codec issues). vo-handout.mp3 measured 17.95s — impossible for its 13-word line and matching the artifacts script — renamed to vo-artifacts.mp3 (fits the 22s slot). Problem card widened 14s to 16s toward the slower narration pace; content now sits exactly at the 175s cap (5115 rendered frames = 170.5s = 2:50.5). Remaining regenerations flagged by validation and left to the narrator: vo-hook (8.54s vs 8s), vo-problem (19.25s vs 16s), vo-build-story (32.42s vs 29s), and the genuinely missing vo-handout line. vo-gestures (9.98s) ducks the clip through ~10.2s, so the live "connect these two" must fall after that point in the take. Typecheck and lint pass.

[HUMAN DECISION] Fit the video to the delivered audio rather than regenerating any narration.
No-regeneration audio fit: measured every narration's real speech extent (each carried ~0.35-0.4s of trailing silence) and trimmed all files losslessly with mp3 stream copy; located the 0.65s sentence gap at 7.35s inside the 17.9s artifacts narration and split it there — part one (7.68s) stays with the artifacts segment and part two (10.24s, the "writes the class handout — recap, glossary, comprehension questions" sentence) becomes the previously missing vo-handout.mp3, matching the VS Code handout visuals exactly. Segments resized to the narration: problem card 19.4s, artifacts 8.5s, handout 10.5s, build-story 32.5s, hook 8.4s. New holdLastFrame mechanism: a segment may outlive its footage — BrowserFrame now wraps the tail in a Freeze on the last real frame instead of unmounting to blank (used by hook +0.4s, handout +3.3s over the static handout page, build-story +3.5s over the static repo page); the validator's short-window rule downgraded from fatal to a draft warning silenced by holdLastFrame: true, and the duration cap now correctly measures the RENDERED length (segments minus transition overlaps) against 175s. Result: 5034 frames = 167.8s (2:48), and validation reports zero problems without any override flag — submission-ready. Verified by smoke-rendering both freeze regions with audio; typecheck and lint pass.

[HUMAN DECISION] Audio is inaudible in Studio, including the screen recordings' own audio; diagnose.
Audio investigation: rendered slices of both audio paths and measured raw samples — narration and live recording audio are strong in the output (peaks 0.93), and the first full render carries a healthy AAC track in every segment, so the composition was never silent; the inaudible playback is Remotion Studio's own mute state (the M shortcut / speaker toggle persists in localStorage) or browser tab/autoplay muting. The per-segment audio map of the full render did expose one real content gap: the gestures take's live "connect these two" sits at 8.0-9.2s (confirmed by interim captions in extracted frames and a matching speech burst in the source audio), where the 9.8s gestures narration both talked over it and ducked it to 15%. Fixed with the existing audio only: vo-gestures.mp3 trimmed losslessly at its 4.35-5.0s sentence gap so narration covers just the two setup sentences and the duck window ends at ~4.9s, leaving the live command at full volume; the third sentence remains as a subtitle while the command executes, and the segment tightened 22s to 19s to end shortly after the "Causes" edge lands (visible by 17s in extracted frames). Rendered length now 4944 frames = 164.8s (2:44.8) with zero validation warnings; typecheck and lint pass.

[HUMAN DECISION] Narrations audibly overlap at segment boundaries ("condensed" audio), and a background music bed (public/audio/background.mp3) should sit quietly under everything.
Narration solo windows and music bed: the overlap was structural — TransitionSeries mounts both segments during each 0.5s crossfade, and several narrations filled their segment to within ~0.3s of the boundary, so the next voiceover began while the previous was still speaking. Every narration now starts NARRATION_DELAY_SEC (0.5s) into its segment via a nested Sequence, and validation requires delay + narration to fit inside the segment, so voiceovers can never coexist across a transition; segments grew accordingly (hook 9.1s, problem 19.9s, handout 11.1s, build-title 4.6s, build-story 33.1s, end-card 8.5s) and all narration-timed cues shifted +0.5s. The duck window follows the delay. background.mp3 (178.7s, RMS 0.107) plays across the whole composition at MUSIC_VOLUME 0.06 — measured against narration RMS 0.05-0.076 this lands the bed ~20dB under the voice — with a 1s fade-in and an end fade-out. Rendered length 5049 frames = 168.3s (2:48.3), zero validation warnings; typecheck and lint pass.
